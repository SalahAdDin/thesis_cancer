class UploadFile {
  final String? id;
  final String? url;
  final String? description;
  final String? postID;
  final String? userID;

  const UploadFile(
      {this.id, this.url, this.description, this.postID, this.userID});
}
